version: 3

dotenv: [".env"]

tasks:
    client:
        dir: ui
        cmds:
            - bun dev

    client:build:
        dir: ui
        cmds:
            - bun run build

    format:
        cmds:
            - bun run format

    server:
        cmds:
            - air
